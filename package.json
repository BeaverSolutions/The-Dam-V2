{
  "name": "beavrdam",
  "version": "2.0.0",
  "description": "BeavrDam — AI-powered sales pipeline by Beaver Solutions",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node index.js",
    "install:all": "cd client && npm install && cd ../server && npm install",
    "dev:server": "cd server && node index.js",
    "dev:client": "cd client && npm run dev",
    "check:post-deploy": "node scripts/post-deploy-autonomy-check.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  }
}
